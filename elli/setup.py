from setuptools import setup

setup(
    name='Elli',
    version='1.0',
    long_description=__doc__,
    packages=['Elli'],
    include_package_data=True,
    zip_safe=False,
    install_requires=[
    ]
)
